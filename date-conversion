/* Converts a date into a full number*/

#include <stdio.h>

int main(void)
{
    int month, day, year;
    
    printf ("Enter the date (mm/dd/yyyy): ");
    scanf ("%d/%d/%d", &month, &day, &year);
    
    printf ("The date is: %d%.2d%.2d\n", year, month, day);
    
    return 0;
}
